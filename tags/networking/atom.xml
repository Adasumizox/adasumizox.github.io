<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>Daniel&#x27;s Detection Notes - Networking</title>
    <subtitle>Hands-on detection engineering notes by Daniel Jeczen: building EDR and SIEM labs, exploring Windows telemetry, and turning security events into useful detections.</subtitle>
    <link rel="self" type="application/atom+xml" href="https://adasumizox.github.io/tags/networking/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://adasumizox.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-09-02T00:00:00+00:00</updated>
    <id>https://adasumizox.github.io/tags/networking/atom.xml</id>
    <entry xml:lang="en">
        <title>Cutting the Network from Inside</title>
        <published>2026-09-02T00:00:00+00:00</published>
        <updated>2026-09-02T00:00:00+00:00</updated>
        
        <author>
          <name>
            
              Unknown
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://adasumizox.github.io/posts/05-cutting-the-network-from-inside/"/>
        <id>https://adasumizox.github.io/posts/05-cutting-the-network-from-inside/</id>
        
        <content type="html" xml:base="https://adasumizox.github.io/posts/05-cutting-the-network-from-inside/">&lt;p&gt;&lt;em&gt;Building an EDR from scratch, essay 5 of 13.&lt;&#x2F;em&gt;&lt;&#x2F;p&gt;
&lt;p&gt;Network isolation is the response action that looks easiest and is hardest.&lt;&#x2F;p&gt;
&lt;p&gt;The pitch is simple enough that a manager can specify it in one sentence: a
machine is compromised, cut it off the network, contain the damage. Every
commercial EDR has the button. It usually has a reassuring icon.&lt;&#x2F;p&gt;
&lt;p&gt;And the implementation is a paradox with a very short fuse. You are going to
tell a machine to stop talking to the network — using the network. The
instruction to sever connectivity arrives over the connectivity it severs.
If you do the obvious thing, the machine goes silent mid-sentence and stays
silent forever, because the only channel you had for saying “actually, let
it back on” was the one you just cut.&lt;&#x2F;p&gt;
&lt;p&gt;That’s not a race condition you’ll catch in code review. It’s a machine that
needs someone to physically walk to it. Multiply by the number of hosts an
analyst can click through in a panic, and a containment feature becomes an
outage.&lt;&#x2F;p&gt;
&lt;p&gt;Everything interesting about this action follows from that one problem.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;isolation-isn-t-block-everything&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#isolation-isn-t-block-everything&quot; aria-label=&quot;Anchor link for: isolation-isn-t-block-everything&quot;&gt;Isolation isn’t “block everything”&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;The naive version — drop all traffic — is unusable, and the exemption list
tells you a lot about how networks actually work. From
&lt;code&gt;shipper&#x2F;src&#x2F;isolate.rs&lt;&#x2F;code&gt;:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! Self-harm is the whole problem (see proto IsolateHostAction): a&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! ruleset that fails to exempt the agent&amp;#39;s own backend endpoint bricks&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! the host — isolated, unreachable, and un-un-isolatable. So the&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! executor refuses to apply ANYTHING unless it can parse and resolve&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! the backend endpoint, and validates every operator CIDR before the&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! kernel is touched (all-or-nothing).&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Start with the refusal, because it’s the most important line. If the agent
cannot determine its own backend endpoint, it applies &lt;em&gt;nothing at all&lt;&#x2F;em&gt;. Not
a best-effort ruleset, not a partial block. The action fails and reports
failure.&lt;&#x2F;p&gt;
&lt;p&gt;This runs against a reflex most of us have — partial progress feels
responsible, and “we contained most of it” sounds better than “we did
nothing.” Here it’s precisely backwards. A partially-applied isolation is a
bricked host, which is strictly worse than an uncontained one: you’ve lost
the machine &lt;em&gt;and&lt;&#x2F;em&gt; your visibility into it, and now the incident includes a
site visit. When a failure mode is unrecoverable, refusing to start is the
safe branch.&lt;&#x2F;p&gt;
&lt;p&gt;The rest of the exemption list is a tour of the protocols people forget:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! kernel is touched (all-or-nothing). The exemption set is deliberately&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! minimal: backend endpoint, loopback, DHCP (v4 + v6, or lease expiry&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! takes the host off the network underneath the containment), and IPv6&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! neighbor discovery (ARP lives below IP, but NDP rides ICMPv6 — block&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! it and v6 connectivity dies, including to the backend).&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;strong&gt;Loopback&lt;&#x2F;strong&gt; — block it and half the software on the machine breaks, because
local services talk to each other over &lt;code&gt;127.0.0.1&lt;&#x2F;code&gt; constantly. You’d contain
the host by breaking it.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;DHCP&lt;&#x2F;strong&gt; is the one that gets people, and it’s a lovely example of a
time-delayed failure. Block DHCP and everything works fine. For hours. Then
the lease expires, the renewal is blocked, the interface loses its address,
and the host drops off the network entirely — including the exempted backend
path, which was exempted by IP on an interface that no longer has one. Your
isolation silently upgrades itself to a brick, long after anyone is watching.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;IPv6 neighbor discovery&lt;&#x2F;strong&gt; is the same trap with a protocol-layering twist.
On IPv4, address resolution is ARP, which sits &lt;em&gt;below&lt;&#x2F;em&gt; IP and isn’t touched
by IP-layer filtering — you can block everything and ARP still works. IPv6
folded that function into NDP, which rides on ICMPv6, which &lt;em&gt;is&lt;&#x2F;em&gt; IP traffic.
Apply the IPv4 mental model to an IPv6 host and you block neighbor discovery,
and v6 connectivity dies — including to your backend, if it’s reached over
v6. Two protocols doing the same job at different layers, and the difference
is only visible if you know the history.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;what-you-refuse-to-exempt&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#what-you-refuse-to-exempt&quot; aria-label=&quot;Anchor link for: what-you-refuse-to-exempt&quot;&gt;What you refuse to exempt&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;The omissions are more interesting than the inclusions, because each one is
a deliberate acceptance of pain:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! it and v6 connectivity dies, including to the backend). Deliberately&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! NOT exempted: established flows (an active C2 session must die) and&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! DNS (re-resolution would open a tunnel; backend IPs are pinned at&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! apply time — a backend failover during isolation needs an operator&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! allow_cidrs entry planned ahead).&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;strong&gt;Established flows must die.&lt;&#x2F;strong&gt; Almost every firewall ruleset on earth
begins with “allow established and related” — it’s the first line of every
tutorial, because without it stateful protocols break. But an active C2
session &lt;em&gt;is&lt;&#x2F;em&gt; an established connection. Exempt established flows and you have
built a containment that specifically permits the thing you’re containing.
The attacker’s live shell survives; only their ability to open &lt;em&gt;new&lt;&#x2F;em&gt;
connections is blocked. This is, I’d bet, a real bug in real products.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;DNS must die&lt;&#x2F;strong&gt;, which hurts more than it sounds. Blocking name resolution
breaks a lot of software in confusing ways. But DNS is a general-purpose
bidirectional channel that every network permits by default — DNS tunneling
is a mature, tooled technique, not a curiosity. Leaving a hole for it
because “it’s just name resolution” defeats the exercise.&lt;&#x2F;p&gt;
&lt;p&gt;The cost is stated plainly rather than hidden: backend IPs are pinned as
addresses at the moment of application, so if the backend fails over to a
different address while a host is isolated, that host can’t follow, and
recovering it needs a CIDR that an operator planned in advance. That’s a
genuine operational constraint. Writing it down in the module header, next to
the decision that causes it, is what separates an accepted trade-off from a
future incident report.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;half-applied-is-worse-than-not-applied&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#half-applied-is-worse-than-not-applied&quot; aria-label=&quot;Anchor link for: half-applied-is-worse-than-not-applied&quot;&gt;Half-applied is worse than not applied&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;Since a partially-applied ruleset is the disaster case, atomicity isn’t a
nice-to-have. There must never be an instant where the block-everything rule
exists and the exemption doesn’t.&lt;&#x2F;p&gt;
&lt;p&gt;What I find genuinely satisfying is that all three platforms provide a way to
do this, and all three arrived at it independently.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Linux&lt;&#x2F;strong&gt; uses an &lt;code&gt;nft -f&lt;&#x2F;code&gt; file load, which applies as a single transaction.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;macOS&lt;&#x2F;strong&gt; uses PF anchors, and the sequencing is spelled out step by step:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; sequenced so the policy only ever becomes visible complete:&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; (1) `pfctl -E` enables PF with a reference token — rules without&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; enforcement would be silent theater, PF ships disabled on macOS;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; (2) the whole ruleset lands in the anchor via one&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; `pfctl -a edr_isolate -f -` load (an anchor load is a single commit,&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; the `nft -f` analog); (3) the main ruleset gets the anchor&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; attachment only if missing — attaching AFTER the load means the&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; first evaluation already sees the full policy;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Step 3 is the one to admire. The anchor is &lt;em&gt;populated first&lt;&#x2F;em&gt;, then attached
to the main ruleset. Attach first and there’s a window — however brief —
where an empty or half-loaded anchor is live. Ordering the operations so the
dangerous window never opens is cheaper and more reliable than trying to make
the window small.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Windows&lt;&#x2F;strong&gt; uses the Windows Filtering Platform:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; mutations run inside one FwpmTransaction — commit or nothing, the&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; `nft -f` atomicity.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Three operating systems, three unrelated APIs, one requirement. When a
constraint shows up identically across independent designs, it’s usually
telling you something true about the problem rather than about the tools.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;rules-don-t-apply-to-conversations-already-happening&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#rules-don-t-apply-to-conversations-already-happening&quot; aria-label=&quot;Anchor link for: rules-don-t-apply-to-conversations-already-happening&quot;&gt;Rules don’t apply to conversations already happening&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;Here’s the concept I’d most want a reader to take away, because it
generalizes far beyond firewalls and it’s counterintuitive until it isn’t.&lt;&#x2F;p&gt;
&lt;p&gt;A stateful firewall doesn’t evaluate rules for every packet. That would be
expensive. Instead, the first packet of a flow is evaluated against the rules,
and the verdict is cached in a &lt;strong&gt;state table&lt;&#x2F;strong&gt;. Subsequent packets of that
flow match the state and skip rule evaluation entirely.&lt;&#x2F;p&gt;
&lt;p&gt;Which means: &lt;strong&gt;changing the rules does not affect conversations already in
progress.&lt;&#x2F;strong&gt; You install a perfect block-everything ruleset, and the attacker’s
existing session sails straight through it, because that session’s packets
never reach the rules at all.&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; first evaluation already sees the full policy; (4) `-F states`&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; flushes the state table, because PF consults states BEFORE rules —&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; a pre-isolation flow&amp;#39;s state would ferry C2 packets straight past&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; the block.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;You have to flush the state table. The ruleset is only half the enforcement;
the cached verdicts are the other half, and they’re older than your policy.&lt;&#x2F;p&gt;
&lt;p&gt;Windows has the same problem and solves it as a side effect, which is a
pleasant piece of luck:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; table and macOS anchor exactly. Adding&#x2F;deleting ALE filters makes&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; BFE reauthorize established connections, so live C2 dies at apply&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; with no extra work.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;WFP’s ALE (Application Layer Enforcement) layers reauthorize existing
connections when filters change. The state invalidation is automatic.&lt;&#x2F;p&gt;
&lt;p&gt;The general shape — &lt;em&gt;a policy change must invalidate the caches that were
built under the old policy&lt;&#x2F;em&gt; — is everywhere once you look. Session tokens
issued before a permission was revoked. Connection pools holding
credentials rotated an hour ago. CDN caches serving content you’ve since
made private. In every case the new rule is correct and completely bypassed,
because something old is short-circuiting the check. Ask, always: what
already decided this, before I changed the rules?&lt;&#x2F;p&gt;
&lt;h2 id=&quot;living-in-someone-else-s-house&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#living-in-someone-else-s-house&quot; aria-label=&quot;Anchor link for: living-in-someone-else-s-house&quot;&gt;Living in someone else’s house&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;An enterprise machine’s firewall is not yours. It has corporate policy,
maybe a VPN client, maybe &lt;code&gt;firewalld&lt;&#x2F;code&gt;, maybe Docker rewriting rules whenever
a container starts. You are a guest.&lt;&#x2F;p&gt;
&lt;p&gt;The pattern that makes this survivable is an owned namespace:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; The agent-owned nftables table (Linux). Created, flushed, and deleted&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; by name only — other tables (firewalld, docker) are never touched.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #FEDE5D;&quot;&gt;pub const&lt;&#x2F;span&gt;&lt;span style=&quot;color: #F97E72;&quot;&gt; TABLE_NAME&lt;&#x2F;span&gt;&lt;span style=&quot;color: #FEDE5D;&quot;&gt;: &amp;amp;&lt;&#x2F;span&gt;&lt;span style=&quot;color: #FE4450;&quot;&gt;str&lt;&#x2F;span&gt;&lt;span style=&quot;color: #FFFFFFEE;&quot;&gt; =&lt;&#x2F;span&gt;&lt;span style=&quot;color: #FF8B39;&quot;&gt; &amp;quot;edr_isolate&amp;quot;&lt;&#x2F;span&gt;&lt;span&gt;;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; The agent-owned PF anchor (macOS) — the same owned-namespace idea as&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; the Linux table: loaded, listed, and flushed by name only. The main&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; ruleset is touched exactly once, to attach the anchor reference&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; (see render_pf_main_ruleset), never to edit other rules.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Windows gets the same treatment with a fixed provider and sublayer GUID. In
all three cases the rule is: everything the agent creates lives under one
name it owns, it only ever manipulates things under that name, and cleanup is
“delete my container” rather than “remove the rules I think I added.”&lt;&#x2F;p&gt;
&lt;p&gt;“Touched exactly once” is the discipline that makes it work. There’s one
unavoidable interaction with shared state — the anchor has to be &lt;em&gt;referenced&lt;&#x2F;em&gt;
from the main ruleset or it’s never consulted — and that single touch is
called out explicitly, so the exception stays an exception.&lt;&#x2F;p&gt;
&lt;p&gt;This is just namespacing, the same instinct that gives you Java packages and
Kubernetes namespaces. It’s worth naming as a general technique for
modifying systems you don’t own: &lt;strong&gt;claim a namespace, confine yourself to
it, and make removal a single operation.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;h2 id=&quot;deciding-what-a-reboot-means&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#deciding-what-a-reboot-means&quot; aria-label=&quot;Anchor link for: deciding-what-a-reboot-means&quot;&gt;Deciding what a reboot means&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;One more decision, easy to skip past, that is actually a policy question
disguised as an API flag:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; `nft -f` atomicity. The engine session is the default non-dynamic&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; one and no filter carries FWPM_FILTER_FLAG_PERSISTENT: containment&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; outlives the agent process but not a reboot, matching the Linux&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;&#x2F; table and macOS anchor exactly.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Should isolation survive a reboot?&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Yes&lt;&#x2F;strong&gt; is defensible: the machine was compromised, containment should be
durable, and an attacker who can reboot shouldn’t be able to escape it.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;No&lt;&#x2F;strong&gt; is what’s implemented, and the reasoning is about recoverability.
Isolation that persists across reboots means a mistake — a bad rule, a
resolution failure, a mis-clicked host — needs manual intervention on the
console. Isolation that dies at reboot means the worst case has a recovery
path any user can perform, and the agent can simply re-apply it on startup if
the host is genuinely still contained.&lt;&#x2F;p&gt;
&lt;p&gt;I don’t think there’s a universal right answer; it depends on whether you
fear attackers with reboot access more than you fear your own mistakes at 2am.
What matters is that the question got asked and answered &lt;em&gt;the same way on all
three platforms&lt;&#x2F;em&gt;. WFP filters can be persistent. The agent deliberately
doesn’t make them so, specifically to match the Linux and macOS behavior.&lt;&#x2F;p&gt;
&lt;p&gt;That’s the discipline that keeps cross-platform systems sane. Every platform
offers slightly different capabilities, and the temptation is to use whatever
each one provides. Do that and you get three subtly different products
wearing one name, and an operator who learned the behavior on Linux is
surprised by Windows during an incident. Choosing the &lt;em&gt;same&lt;&#x2F;em&gt; semantics
everywhere — even when it means declining a feature — is what makes fleet
behavior predictable.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;testing-the-thing-you-can-t-test&quot;&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#testing-the-thing-you-can-t-test&quot; aria-label=&quot;Anchor link for: testing-the-thing-you-can-t-test&quot;&gt;Testing the thing you can’t test&lt;&#x2F;a&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;Last piece, and it’s the one that made this action tractable at all:&lt;&#x2F;p&gt;
&lt;pre class=&quot;giallo&quot; style=&quot;color: #FFFFFF; background-color: #262335;&quot;&gt;&lt;code data-lang=&quot;rust&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! Policy is separated from mechanism so the dangerous decision logic is&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;! exhaustively unit-tested without ever touching a firewall:&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;!   * `IsolateControl` — the OS primitive (atomically replace &#x2F; remove&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;!     the containment ruleset), one thin impl per platform.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;!   * `IsolateExecutor` — the pure guard&#x2F;rendering logic over any&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span style=&quot;color: #848BBD;font-style: italic;&quot;&gt;&#x2F;&#x2F;!     `IsolateControl`, driven by a mock in tests.&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The dangerous part of this feature isn’t the firewall call. It’s the
&lt;em&gt;decisions&lt;&#x2F;em&gt;: which exemptions to include, whether the backend resolved, what
to do about a malformed CIDR, what the ruleset text should say. All of that
is pure computation — inputs to strings — and pure computation can be tested
exhaustively, on any machine, in milliseconds, with no root and no risk.&lt;&#x2F;p&gt;
&lt;p&gt;So the rendering functions produce ruleset text that gets compared against
golden files, and the guard logic runs against a mock. The part that actually
touches the kernel shrinks to a thin layer whose only job is to hand a
prepared string to &lt;code&gt;nft&lt;&#x2F;code&gt;, &lt;code&gt;pfctl&lt;&#x2F;code&gt;, or BFE.&lt;&#x2F;p&gt;
&lt;p&gt;Which means most of this feature’s correctness was established on a Linux
laptop, including the Windows and macOS behavior. Not all of it — you still
need real drills to learn things like PF’s state-table ordering — but the
decision logic, which is where the bricking bugs live, is verified without a
firewall in sight.&lt;&#x2F;p&gt;
&lt;p&gt;That inversion, and how far you can push it when your target is a kernel you
cannot attach a debugger to, returns later in the series.&lt;&#x2F;p&gt;
&lt;p&gt;But first, the uncomfortable inverse of everything this series has covered so
far: not what the sensors see, but what they structurally cannot. That is
the subject of the next essay.&lt;&#x2F;p&gt;
</content>
        
    </entry>
</feed>
